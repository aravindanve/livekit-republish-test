{
  "name": "livekit-republish-test",
  "version": "1.0.0",
  "description": "Demonstrates a bug where livekit publish request times out",
  "main": "index.js",
  "scripts": {
    "start": "npm-run-all server client",
    "stop": "docker-compose -f docker-compose.yml down",
    "server": "docker-compose -f docker-compose.yml up -d",
    "client": "npx serve public -l 3000"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "npm-run-all": "^4.1.5",
    "serve": "^14.0.1"
  }
}
